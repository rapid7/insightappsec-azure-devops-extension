{
  "description": "The package.json for unit tests.",
  "devDependencies": {
    "@types/jest": "^26.0.22",
    "@types/node": "^16.4.12",
    "jest": "^26.6.3",
    "ts-jest": "^26.5.5",
    "typescript": "^4.2.4"
  },
  "scripts": {
    "test": "jest",
    "coverage": "jest --coverage"
  }
}
